# A simple server application revision deployment via AWS CodeDeploy and Github
A simple application revision deployment (ex: Apache httpd) from GitHub to a Linux server instance (ex: RHEL) via AWS CodeDeploy.
